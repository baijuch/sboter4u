<launch>

  <!-- Set the param that trajectory_execution_manager needs to find the controller plugin -->
  <param name="moveit_controller_manager" value="sboter4u_controller_manager/MoveItControllerManager"/>

  <!-- The rest of the params are specific to this plugin -->
  <rosparam file="$(find sboter4u_config_gen)/config/fake_controllers.yaml"/>

</launch>
